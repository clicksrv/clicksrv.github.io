import styled from 'styled-components'

import { black } from '../colors'

const EditorSidePaneTitle = styled.h3`
  color: ${black};
  font-size: 17px;
  font-weight: 500;

  margin: 0;
`

export default EditorSidePaneTitle
